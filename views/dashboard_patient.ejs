<%- include('includes/header') %>

<div class="row">
  <h1>Avaliado</h1>
  <a class="button pull-right" href="/meetings/book">+ Agendar entrevista</a>
</div>

<% if (meetings.current.length > 0) { %>
<h3>Entrevista</h3>
<div class="column meeting-strip">
  <% for (var m of meetings.current) { %>
    <div class="row meeting-item">
      <div class="meeting-item-time grow"><time><%= m.start_time %><time></div>
      <div><a class="button" href="/meetings/join/<%= m.id %>">Iniciar entrevista</a></div>
    </div>
  <% } %>
</div>
<% } %>

<h2>Agendamentos</h2>

<% if (meetings.upcoming.length > 0) { %>
  <div class="column meeting-strip">
    <% for (var m of meetings.upcoming) { %>
      <div class="row meeting-item">
        <div class="meeting-item-time grow"><time><%= m.start_time %></time></div>
      </div>
    <% } %>
  </div>
<% } else { %>

  <p>Voce nao possui agendamento. Voce pode <a href="/meetings/book">agendar uma entrevista</a>.</p>

<% } %>

<%- include('includes/footer') %>
