<%- include('includes/header') %>

<div class="row">
  <h2>Painel do Consultor</h2>
  <a class="button pull-right" href="/meetings/create">+ Adicionar disponibilidade</a>
</div>

<% if (meetings.current.length > 0) { %>
<h3>Entrevista atual</h3>
<div class="column meeting-strip">
  <% for (var m of meetings.current) { %>
    <% if (m.booked) { %>
      <div class="row meeting-item">
        <div class="meeting-item-time grow"><time><%= m.start_time %></time></div>
        <div class="pull-right"><a class="button" href="/meetings/join/<%= m.id %>">Iniciar entrevista</a></div>
      </div>
    <% } else { %>
      <div class="row meeting-item unclaimed">
        <div class="meeting-item-time grow"><time><%= m.start_time %></time></div>
        <div><span class="button secondary disabled">Sem agendamento</span></div>
      </div>
    <% } %>
  <% } %>
</div>
<% } %>


<h3>Disponibilidades</h3>

<% if (meetings.upcoming.length > 0) { %>
  <div class="column meeting-strip">
    <% for (var m of meetings.upcoming) { %>
      <% if (m.booked) { %>
        <div class="row meeting-item">
          <div class="meeting-item-time grow"><time><%= m.start_time %></time></div>
          <div><span class="button disabled">Agendada</span></div>
        </div>
      <% } else { %>
        <div class="row meeting-item unclaimed">
          <div class="meeting-item-time grow"><time><%= m.start_time %><time></div>
          <div><span class="button secondary disabled">Sem agendamento</span></div>
        </div>
      <% } %>
    <% } %>
  </div>

<% } else { %>

  <p>Voce nao possui entrevistas agendadas. <a href="/meetings/create">Agendamento</a>.</p>

<% } %>

<%- include('includes/footer') %>
